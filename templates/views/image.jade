extends ../layouts/default

block content
	.container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
		article
			if data.public_id
				h1 Imagen
				
				img(src=locals.data.results.url)
				
				p= ('Image ID: '+data.results.public_id)
				p= ('Tags: '+data.tagss)
				
			else
				p lol not working
			